# Lesson M-838 — Expected value by integration

Part T. T. Probability · **Continuous random variables** · High

Prerequisites: `T.2-F.2`.


This lesson isolates **Expected value by integration** and trains it until it is automatic.

This is a High skill: it is standard GRE material. You should be able to do it without warming up. Probability is a solid additional-topics block: Bayes, expectation, named distributions, CLT.

## 1. Why this skill exists

If you cannot do **Expected value by integration**, every later item that secretly uses it becomes a two-minute panic. The GRE will not name the skill. It will drop you into a computation or a “which statement is true?” and wait.

## 2. From zero

### Landscape of *Continuous random variables*

PDF integrates to $1$, $P(X=x)=0$, CDF is an integral of the PDF, expectation $\int x f(x)\,dx$. Uniform, exponential, normal as first models.

### This skill, specifically

PDF integrates to $1$, $P(X=x)=0$, CDF is an integral of the PDF, expectation $\int x f(x)\,dx$.

You are not learning the whole topic again. You are learning the exact move named in the title. Neighboring lessons exist; use them if a word here is still foreign, then come back.

## 3. Precise statement

If $X$ has PDF $f$, $\mathbb{E}[X]=\int_{-\infty}^{\infty} x f(x)\,dx$ when $\int|x|f<\infty$. LOTUS: $\mathbb{E}[g(X)]=\int g(x)f(x)\,dx$. For nonnegative continuous $X$, also $\mathbb{E}[X]=\int_0^\infty (1-F(x))\,dx$.

Copy that statement onto paper and replace each symbol with a tiny example. A definition you cannot instantiate is a definition you do not have.

## 4. How a working mathematician uses it

Write the hypotheses, apply the move, check the conclusion against the question asked.

On the exam, this should feel like a reflex: see the pattern, apply the move, check hypotheses, box the quantity asked for.

## 5. Worked example

**Problem.** $f(x)=1/6$ on $[0,6]$. $\mathbb{E}[X]$.

**Solution.** Midpoint $3$, or $\int_0^6 x/6\,dx=3$.

Now cover the solution and redo it. If you cannot, you are still in Section 2.

## 6. GRE-style problem

**Problem.** $f(x)=2x$ on $[0,1]$. $\mathbb{E}[X]$ and $\mathbb{E}[X^2]$.

**Solution.** $\int_0^1 2x^2\,dx=2/3$; $\int_0^1 2x^3\,dx=1/2$.

Notice what was *not* done: no extra theory, no calculator, no essay. The governing idea, then the computation.

## 7. Traps that cost points

- Writing $\mathbb{E}[X]=\int F(x)\,dx$, or using $\sum x p(x)$ on a PDF.
- Skip checking a candidate in the original equation.

## 8. Drills

Work these closed-book. Then check.

**Drill 1.** Nonnegative $X$ with $F(x)=1-1/(1+x)$ for $x\ge 0$. $\mathbb{E}[X]$ via tail.

**Solution.** $\int_0^\infty \frac{1}{1+x}\,dx=\infty$ (no finite mean).

Cover that solution, redo the drill on paper, then continue.

**Drill 2.** $\mathbb{E}[3X+2]$ if $\mathbb{E}[X]=4$.

**Solution.** $14$.

## 9. Lock it in

You own this skill when you can do Drill 1 and Drill 2 tomorrow, mixed among unrelated problems, in under five minutes each, with the traps named in Section 7 avoided on purpose.

Previous: [M-837](/lesson/M-837) — CDF.

Next lesson: [M-839](/lesson/M-839) — Variance by integration.

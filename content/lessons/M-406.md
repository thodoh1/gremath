# Lesson M-406 — First-order systems

Part J. J. Differential Equations · **Systems of ODEs** · High

Prerequisites: `K.7-J.2`.


This lesson isolates **First-order systems** and trains it until it is automatic.

This is a High skill: it is standard GRE material. You should be able to do it without warming up. ODEs appear as additional or calculus-adjacent items: separable, linear first order, characteristic equations.

## 1. Why this skill exists

If you cannot do **First-order systems**, every later item that secretly uses it becomes a two-minute panic. The GRE will not name the skill. It will drop you into a computation or a “which statement is true?” and wait.

## 2. From zero

### Landscape of *Systems of ODEs*

Write $\mathbf{x}'=A\mathbf{x}$, then eigenvalues of $A$ produce the modes. Phase portraits are the picture. Nonlinear systems linearize at equilibria.

### This skill, specifically

Write $\mathbf{x}'=A\mathbf{x}$, then eigenvalues of $A$ produce the modes.

You are not learning the whole topic again. You are learning the exact move named in the title. Neighboring lessons exist; use them if a word here is still foreign, then come back.

## 3. Precise statement

A first-order system is $\mathbf{x}'=\mathbf{F}(t,\mathbf{x})$. A second-order scalar ODE $y''=f(t,y,y')$ becomes a system via $x_1=y$, $x_2=y'$, so $x_1'=x_2$, $x_2'=f(t,x_1,x_2)$. Linear constant-coefficient systems are $\mathbf{x}'=A\mathbf{x}+\mathbf{g}(t)$.

Copy that statement onto paper and replace each symbol with a tiny example. A definition you cannot instantiate is a definition you do not have.

## 4. How a working mathematician uses it

Write the hypotheses, apply the move, check the conclusion against the question asked.

On the exam, this should feel like a reflex: see the pattern, apply the move, check hypotheses, box the quantity asked for.

## 5. Worked example

**Problem.** Convert $y''+y=0$ to a first-order system.

**Solution.** $x_1=y$, $x_2=y'$: $x_1'=x_2$, $x_2'=-x_1$. In matrix form $\mathbf{x}'=\begin{pmatrix}0&1\\-1&0\end{pmatrix}\mathbf{x}$.

Now cover the solution and redo it. If you cannot, you are still in Section 2.

## 6. GRE-style problem

**Problem.** The system $x'=y$, $y'=-x$ is equivalent to

**Solution.** $x''+x=0$.

Notice what was *not* done: no extra theory, no calculator, no essay. The governing idea, then the computation.

## 7. Traps that cost points

- Introducing $x_1=y'$ and $x_2=y$ but writing $x_1'=x_2$ (the assignments are backwards).
- Skip checking a candidate in the original equation.

## 8. Drills

Work these closed-book. Then check.

**Drill 1.** Convert $y''-3y'+2y=0$ to $\mathbf{x}'=A\mathbf{x}$.

**Solution.** $A=\begin{pmatrix}0&1\\-2&3\end{pmatrix}$ with $\mathbf{x}=(y,y')$.

Cover that solution, redo the drill on paper, then continue.

**Drill 2.** Dimension of the first-order system from an $n$th-order scalar ODE?

**Solution.** $n$.

## 9. Lock it in

You own this skill when you can do Drill 1 and Drill 2 tomorrow, mixed among unrelated problems, in under five minutes each, with the traps named in Section 7 avoided on purpose.

Previous: [M-405](/lesson/M-405) — Variation of parameters.

Next lesson: [M-407](/lesson/M-407) — Matrix form of systems.

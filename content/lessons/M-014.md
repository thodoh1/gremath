# Lesson M-014 — Definition and laws of logarithms

Part A. A. Foundations & Algebraic Fluency · **Exponents & logarithms** · Critical

Prerequisites: `A.1`.


Change of base is coming; the laws must already be muscle.

This is a Critical skill: it is used constantly, and a hole here leaks points all over the paper. Algebraic fluency is not a listed ETS bucket of its own, but it is the cost of every calculus and algebra item. Expect it to be invisible when you have it and fatal when you do not.

## 1. Why this skill exists

If you cannot do **Definition and laws of logarithms**, every later item that secretly uses it becomes a two-minute panic. The GRE will not name the skill. It will drop you into a computation or a “which statement is true?” and wait.

## 2. From zero

### Landscape of *Exponents & logarithms*

Exponentiation $a^x$ (for $a>0$) turns addition of exponents into multiplication of powers. Logarithms invert that: $\log_a$ eats a product and returns a sum. The two languages are the same mathematics. Domain restrictions (positive arguments, base $a>0$, $a\neq 1$) are not footnotes; they delete GRE answer choices. Master same-base rewriting, the three log laws, change of base, and monotonicity for inequalities.

### This skill, specifically

A logarithm is an exponent. Every log law is an exponent law run backwards.

You are not learning the whole topic again. You are learning the exact move named in the title. Neighboring lessons exist; use them if a word here is still foreign, then come back.

## 3. Precise statement

For $a>0$, $a\\neq 1$, $\\log_a$ is the inverse of $x\\mapsto a^x$: $\\log_a(a^x)=x$ and $a^{\\log_a x}=x$ for $x>0$. Laws: $\\log_a(xy)=\\log_a x+\\log_a y$, $\\log_a(x/y)=\\log_a x-\\log_a y$, $\\log_a(x^k)=k\\log_a x$.

Copy that statement onto paper and replace each symbol with a tiny example. A definition you cannot instantiate is a definition you do not have.

## 4. How a working mathematician uses it

Domain: argument $>0$, base $>0$ and $\neq 1$. Convert products to sums only after checking positivity.

On the exam, this should feel like a reflex: see the pattern, apply the move, check hypotheses, box the quantity asked for.

## 5. Worked example

**Problem.** Simplify $\\log_2 8+\\log_2(1/2)$.

**Solution.** $3+(-1)=2$, or $\\log_2(8\\cdot 1/2)=\\log_2 4=2$.

Now cover the solution and redo it. If you cannot, you are still in Section 2.

## 6. GRE-style problem

**Problem.** $\\log_3 9^x$ equals

**Solution.** $x\\log_3 9=2x$.

Notice what was *not* done: no extra theory, no calculator, no essay. The governing idea, then the computation.

## 7. Traps that cost points

- $\log(a+b)=\log a+\log b$.
- Logs of nonpositive numbers in $\mathbb{R}$.

## 8. Drills

Work these closed-book. Then check.

**Drill 1.** Compute $\\log_5 1$, $\\log_5 5$, $\\log_5 25$.

**Solution.** $0,1,2$.

Cover that solution, redo the drill on paper, then continue.

**Drill 2.** Solve $\\log_2 x=0$.

**Solution.** $x=1$.

## 9. Lock it in

You own this skill when you can do Drill 1 and Drill 2 tomorrow, mixed among unrelated problems, in under five minutes each, with the traps named in Section 7 avoided on purpose.

Previous: [M-013](/lesson/M-013) — Scientific exponents.

Next lesson: [M-015](/lesson/M-015) — Change-of-base formula.

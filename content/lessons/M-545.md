# Lesson M-545 — Generators

Part M. M. Group Theory · **Groups** · High

Prerequisites: `A.1-Q.1`.


This lesson isolates **Generators** and trains it until it is automatic.

This is a High skill: it is standard GRE material. You should be able to do it without warming up. Group theory is the main abstract-algebra GRE diet: orders, Lagrange, homomorphisms.

## 1. Why this skill exists

If you cannot do **Generators**, every later item that secretly uses it becomes a two-minute panic. The GRE will not name the skill. It will drop you into a computation or a “which statement is true?” and wait.

## 2. From zero

### Landscape of *Groups*

A group is a set with an associative binary operation, identity, and inverses. Abelian means commutative. Subgroups, cyclic groups, orders of elements and of the group: Lagrange is coming.

### This skill, specifically

A group is a set with an associative binary operation, identity, and inverses.

You are not learning the whole topic again. You are learning the exact move named in the title. Neighboring lessons exist; use them if a word here is still foreign, then come back.

## 3. Precise statement

An element $g$ generates $G$ if $\langle g\rangle=G$. For $\mathbb{Z}/n\mathbb{Z}$, $[k]$ generates iff $\gcd(k,n)=1$; there are $\phi(n)$ generators. For $\langle g\rangle$ of order $n$, $g^k$ generates iff $\gcd(k,n)=1$. Powers: $\langle g^k\rangle$ has order $n/\gcd(k,n)$.

Copy that statement onto paper and replace each symbol with a tiny example. A definition you cannot instantiate is a definition you do not have.

## 4. How a working mathematician uses it

Write the hypotheses, apply the move, check the conclusion against the question asked.

On the exam, this should feel like a reflex: see the pattern, apply the move, check hypotheses, box the quantity asked for.

## 5. Worked example

**Problem.** List the generators of $\mathbb{Z}/12\mathbb{Z}$.

**Solution.** $[1],[5],[7],[11]$: the units mod $12$. Check: $\langle 5\rangle=\{0,5,10,3,8,1,\ldots\}$ fills all.

Now cover the solution and redo it. If you cannot, you are still in Section 2.

## 6. GRE-style problem

**Problem.** Number of generators of a cyclic group of order $10$

**Solution.** $\phi(10)=4$.

Notice what was *not* done: no extra theory, no calculator, no essay. The governing idea, then the computation.

## 7. Traps that cost points

- Thinking $2$ generates $\mathbb{Z}/8\mathbb{Z}$ (order of $2$ is $4$, not $8$).
- Skip checking a candidate in the original equation.

## 8. Drills

Work these closed-book. Then check.

**Drill 1.** Does $3$ generate $(\mathbb{Z}/7\mathbb{Z})^\times$?

**Solution.** Yes: it is a primitive root.

Cover that solution, redo the drill on paper, then continue.

**Drill 2.** Order of $g^2$ if $|g|=12$.

**Solution.** $12/\gcd(2,12)=6$.

## 9. Lock it in

You own this skill when you can do Drill 1 and Drill 2 tomorrow, mixed among unrelated problems, in under five minutes each, with the traps named in Section 7 avoided on purpose.

Previous: [M-544](/lesson/M-544) — Cyclic groups.

Next lesson: [M-546](/lesson/M-546) — Order of an element.

# Lesson M-341 — Jacobian matrix

Part H. H. Multivariable Calculus · **Differentiability** · High

Prerequisites: `H.1-K.4`.


This lesson isolates **Jacobian matrix** and trains it until it is automatic.

This is a High skill: it is standard GRE material. You should be able to do it without warming up. Multivariable calculus is explicitly in the calculus portion: partials, gradients, multiple integrals, Jacobians.

## 1. Why this skill exists

If you cannot do **Jacobian matrix**, every later item that secretly uses it becomes a two-minute panic. The GRE will not name the skill. It will drop you into a computation or a “which statement is true?” and wait.

## 2. From zero

### Landscape of *Differentiability*

Existence of partials is not differentiability. The derivative is a linear map (Jacobian). Directional derivatives are $\nabla f\cdot u$. The chain rule in several variables is matrix multiplication of Jacobians.

### This skill, specifically

Existence of partials is not differentiability.

You are not learning the whole topic again. You are learning the exact move named in the title. Neighboring lessons exist; use them if a word here is still foreign, then come back.

## 3. Precise statement

The Jacobian matrix of $\mathbf{F}:\mathbb{R}^n\to\mathbb{R}^m$ is the $m\times n$ matrix of partials $DF_{ij}=\partial F_i/\partial x_j$. For $n=m$, $\det DF$ is the Jacobian determinant, the local volume scale factor (absolute value) in change of variables.

Copy that statement onto paper and replace each symbol with a tiny example. A definition you cannot instantiate is a definition you do not have.

## 4. How a working mathematician uses it

Write the hypotheses, apply the move, check the conclusion against the question asked.

On the exam, this should feel like a reflex: see the pattern, apply the move, check hypotheses, box the quantity asked for.

## 5. Worked example

**Problem.** $F(x,y)=(x^2,xy)$. Jacobian matrix.

**Solution.** $\begin{pmatrix}2x&0\\ y&x\end{pmatrix}$.

Now cover the solution and redo it. If you cannot, you are still in Section 2.

## 6. GRE-style problem

**Problem.** Polar $x=r\cos\theta$, $y=r\sin\theta$, $\det D$

**Solution.** $r$.

Notice what was *not* done: no extra theory, no calculator, no essay. The governing idea, then the computation.

## 7. Traps that cost points

- Mixing Jacobian matrix with gradient (gradient is the Jacobian of a scalar function, as a row or column).
- Skip checking a candidate in the original equation.

## 8. Drills

Work these closed-book. Then check.

**Drill 1.** $F(r,\theta)=(r\cos\theta,r\sin\theta)$, $DF$ at $(1,0)$.

**Solution.** $\begin{pmatrix}\cos 0&-r\sin 0\\ \sin 0& r\cos 0\end{pmatrix}=\begin{pmatrix}1&0\\0&1\end{pmatrix}$.

Cover that solution, redo the drill on paper, then continue.

**Drill 2.** Linear map $\mathbf{x}\mapsto A\mathbf{x}$ has Jacobian

**Solution.** $A$ (constant).

## 9. Lock it in

You own this skill when you can do Drill 1 and Drill 2 tomorrow, mixed among unrelated problems, in under five minutes each, with the traps named in Section 7 avoided on purpose.

Previous: [M-340](/lesson/M-340) — Total differential.

Next lesson: [M-342](/lesson/M-342) — Chain rule in several variables.

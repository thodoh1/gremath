# Lesson M-373 — Fundamental theorem for line integrals

Part I. I. Vector Calculus · **Line integrals** · High

Prerequisites: `I.1`.


This lesson isolates **Fundamental theorem for line integrals** and trains it until it is automatic.

This is a High skill: it is standard GRE material. You should be able to do it without warming up. Vector calculus (Green/Stokes/divergence) appears less often than single-variable calculus but is a high-leverage discriminator.

## 1. Why this skill exists

If you cannot do **Fundamental theorem for line integrals**, every later item that secretly uses it becomes a two-minute panic. The GRE will not name the skill. It will drop you into a computation or a “which statement is true?” and wait.

## 2. From zero

### Landscape of *Line integrals*

Scalar line integrals weight by arc length. Vector line integrals measure work $\int_C\mathbf{F}\cdot d\mathbf{r}$. Conservative fields are gradients: path independence, $\mathbf{F}=\nabla f$, curl zero (on simply connected domains).

### This skill, specifically

Scalar line integrals weight by arc length.

You are not learning the whole topic again. You are learning the exact move named in the title. Neighboring lessons exist; use them if a word here is still foreign, then come back.

## 3. Precise statement

Fundamental theorem for line integrals: if $C$ runs from $A$ to $B$ and $f$ is $C^1$, then $\int_C\nabla f\cdot d\mathbf{r}=f(B)-f(A)$. This is the FTC on the composite $f(\mathbf{r}(t))$. Closed curves give $0$.

Copy that statement onto paper and replace each symbol with a tiny example. A definition you cannot instantiate is a definition you do not have.

## 4. How a working mathematician uses it

Write the hypotheses, apply the move, check the conclusion against the question asked.

On the exam, this should feel like a reflex: see the pattern, apply the move, check hypotheses, box the quantity asked for.

## 5. Worked example

**Problem.** $f=xyz$ from $A=(1,1,1)$ to $B=(2,3,1)$. Compute $\int_C\nabla f\cdot d\mathbf{r}$.

**Solution.** $f(B)-f(A)=6-1=5$, for any $C$ from $A$ to $B$.

Now cover the solution and redo it. If you cannot, you are still in Section 2.

## 6. GRE-style problem

**Problem.** $\int_C\nabla(x^2+y^2)\cdot d\mathbf{r}$ around the unit circle is

**Solution.** $0$.

Notice what was *not* done: no extra theory, no calculator, no essay. The governing idea, then the computation.

## 7. Traps that cost points

- Evaluating $f(A)-f(B)$ (wrong order) or using $f$ when the field is not $\nabla f$.
- Skip checking a candidate in the original equation.

## 8. Drills

Work these closed-book. Then check.

**Drill 1.** $f=e^x\sin y$ from $(0,0)$ to $(0,\pi)$.

**Solution.** $0-0=0$.

Cover that solution, redo the drill on paper, then continue.

**Drill 2.** If $C$ is closed and $f$ is single-valued and $C^1$ on a neighborhood of $C$, the integral is

**Solution.** $0$.

## 9. Lock it in

You own this skill when you can do Drill 1 and Drill 2 tomorrow, mixed among unrelated problems, in under five minutes each, with the traps named in Section 7 avoided on purpose.

Previous: [M-372](/lesson/M-372) — Path independence.

Next lesson: [M-374](/lesson/M-374) — Gradient.

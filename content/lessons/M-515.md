# Lesson M-515 — Systems of congruences

Part L. L. Number Theory · **Congruences** · High

Prerequisites: `L.1`.


This lesson isolates **Systems of congruences** and trains it until it is automatic.

This is a High skill: it is standard GRE material. You should be able to do it without warming up. Number theory is additional-topics / algebra: congruences, Euler, CRT.

## 1. Why this skill exists

If you cannot do **Systems of congruences**, every later item that secretly uses it becomes a two-minute panic. The GRE will not name the skill. It will drop you into a computation or a “which statement is true?” and wait.

## 2. From zero

### Landscape of *Congruences*

$a\equiv b\pmod n$ means $n\mid(a-b)$. Residue classes form $\mathbb{Z}/n\mathbb{Z}$. Inverses exist iff coprime to $n$. Linear congruences and CRT are the solving machine. Fast exponentiation is repeated squaring.

### This skill, specifically

$a\equiv b\pmod n$ means $n\mid(a-b)$.

You are not learning the whole topic again. You are learning the exact move named in the title. Neighboring lessons exist; use them if a word here is still foreign, then come back.

## 3. Precise statement

A general system $x\equiv a_i\pmod{n_i}$ is solvable iff $a_i\equiv a_j\pmod{\gcd(n_i,n_j)}$ for all pairs. The solution, if it exists, is unique modulo $\mathrm{lcm}(n_1,\ldots,n_k)$. Pairwise coprime moduli recover classical CRT and modulus $\prod n_i$.

Copy that statement onto paper and replace each symbol with a tiny example. A definition you cannot instantiate is a definition you do not have.

## 4. How a working mathematician uses it

Write the hypotheses, apply the move, check the conclusion against the question asked.

On the exam, this should feel like a reflex: see the pattern, apply the move, check hypotheses, box the quantity asked for.

## 5. Worked example

**Problem.** Solve $x\equiv 2\pmod{4}$, $x\equiv 4\pmod{6}$.

**Solution.** $\gcd(4,6)=2$ divides $2-4=-2$, so solvable; modulus $\mathrm{lcm}(4,6)=12$. $x=6t+4$, then $6t+4\equiv 2\pmod{4}\Rightarrow 2t\equiv 2\pmod{4}\Rightarrow t\equiv 1\pmod{2}$, so $x\equiv 10\pmod{12}$.

Now cover the solution and redo it. If you cannot, you are still in Section 2.

## 6. GRE-style problem

**Problem.** $x\equiv 1\pmod{4}$, $x\equiv 3\pmod{6}$ is

**Solution.** inconsistent: $1\not\equiv 3\pmod{2}$.

Notice what was *not* done: no extra theory, no calculator, no essay. The governing idea, then the computation.

## 7. Traps that cost points

- Using modulus $n_1 n_2$ when $\gcd>1$ (the true period is the lcm, strictly smaller).
- Skip checking a candidate in the original equation.

## 8. Drills

Work these closed-book. Then check.

**Drill 1.** $x\equiv 1\pmod{6}$, $x\equiv 7\pmod{10}$.

**Solution.** Compatible: $1\equiv 7\pmod{2}$. Then $x=10k+7\equiv 1\pmod{6}\Rightarrow 4k\equiv 0\pmod{6}\Rightarrow k\equiv 0\pmod{3}$, so $x\equiv 7\pmod{30}$.

Cover that solution, redo the drill on paper, then continue.

**Drill 2.** Three congruences $x\equiv 1\pmod{2}$, $x\equiv 1\pmod{3}$, $x\equiv 1\pmod{5}$.

**Solution.** $x\equiv 1\pmod{30}$.

## 9. Lock it in

You own this skill when you can do Drill 1 and Drill 2 tomorrow, mixed among unrelated problems, in under five minutes each, with the traps named in Section 7 avoided on purpose.

Previous: [M-514](/lesson/M-514) — Chinese remainder theorem.

Next lesson: [M-516](/lesson/M-516) — Exponentiation modulo n.

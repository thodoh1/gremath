# Lesson M-590 — Kernel and image

Part N. N. Rings, Modules & Fields · **Rings** · Medium

Prerequisites: `M.4-L.2`.


This lesson isolates **Kernel and image** and trains it until it is automatic.

This is a Medium skill: it appears less often, but when it appears it is often a discriminator. Rings and fields appear as recognition and finite examples, especially $\mathbb{Z}/n\mathbb{Z}$ and $F[x]$.

## 1. Why this skill exists

If you cannot do **Kernel and image**, every later item that secretly uses it becomes a two-minute panic. The GRE will not name the skill. It will drop you into a computation or a “which statement is true?” and wait.

## 2. From zero

### Landscape of *Rings*

Two operations, distributive. Commutative rings, identity, subrings, domains, units, zero-divisors, homomorphisms. $\mathbb{Z}/n\mathbb{Z}$ is the GRE’s favorite ring.

### This skill, specifically

Two operations, distributive.

You are not learning the whole topic again. You are learning the exact move named in the title. Neighboring lessons exist; use them if a word here is still foreign, then come back.

## 3. Precise statement

For a ring hom $\phi:R\to S$, $\ker\phi=\{r:\phi(r)=0\}$ is an ideal of $R$, and $\mathrm{im}\,\phi$ is a subring of $S$. $\phi$ is injective iff $\ker=0$. First iso theorem: $R/\ker\phi\cong\mathrm{im}\,\phi$. Unital homs have $\mathrm{im}$ unital with the same $1$ as $S$ only if $\phi(1)=1$; image then contains $1_S$.

Copy that statement onto paper and replace each symbol with a tiny example. A definition you cannot instantiate is a definition you do not have.

## 4. How a working mathematician uses it

Write the hypotheses, apply the move, check the conclusion against the question asked.

On the exam, this should feel like a reflex: see the pattern, apply the move, check hypotheses, box the quantity asked for.

## 5. Worked example

**Problem.** Kernel and image of $\phi:\mathbb{Z}\to\mathbb{Z}/12\mathbb{Z}$, $\phi(n)=4n\bmod 12$.

**Solution.** Not unital. $\ker=\{n:12\mid 4n\}=\{n:3\mid n\}=3\mathbb{Z}$. Image $\{0,4,8\}$, a subring (without $1$ of $\mathbb{Z}/12$).

Now cover the solution and redo it. If you cannot, you are still in Section 2.

## 6. GRE-style problem

**Problem.** Kernel of evaluation $\mathbb{R}[x]\to\mathbb{R}$, $f\mapsto f(0)$

**Solution.** the ideal $(x)$ of polynomials with zero constant term.

Notice what was *not* done: no extra theory, no calculator, no essay. The governing idea, then the computation.

## 7. Traps that cost points

- Claiming the kernel is always a subring sharing $1$ (it contains $1$ iff $\phi$ is zero).
- Skip checking a candidate in the original equation.

## 8. Drills

Work these closed-book. Then check.

**Drill 1.** If $\ker\phi=R$, then

**Solution.** $\phi=0$ and $\mathrm{im}=\{0\}$.

Cover that solution, redo the drill on paper, then continue.

**Drill 2.** Image of $\mathbb{Z}\to\mathbb{Q}$ the inclusion.

**Solution.** $\mathbb{Z}$, a subring of $\mathbb{Q}$.

## 9. Lock it in

You own this skill when you can do Drill 1 and Drill 2 tomorrow, mixed among unrelated problems, in under five minutes each, with the traps named in Section 7 avoided on purpose.

Previous: [M-589](/lesson/M-589) — Ring homomorphisms.

Next lesson: [M-591](/lesson/M-591) — Ideal definition.

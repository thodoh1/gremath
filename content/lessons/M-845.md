# Lesson M-845 — Conditional distributions

Part T. T. Probability · **Joint distributions** · High

Prerequisites: `T.2-T.3`.


This lesson isolates **Conditional distributions** and trains it until it is automatic.

This is a High skill: it is standard GRE material. You should be able to do it without warming up. Probability is a solid additional-topics block: Bayes, expectation, named distributions, CLT.

## 1. Why this skill exists

If you cannot do **Conditional distributions**, every later item that secretly uses it becomes a two-minute panic. The GRE will not name the skill. It will drop you into a computation or a “which statement is true?” and wait.

## 2. From zero

### Landscape of *Joint distributions*

Joint PMF/PDF, marginals by summing/integrating, conditionals, independence as factorization, covariance matrices, $E[X|Y]$.

### This skill, specifically

Joint PMF/PDF, marginals by summing/integrating, conditionals, independence as factorization, covariance matrices, $E[X|Y]$.

You are not learning the whole topic again. You are learning the exact move named in the title. Neighboring lessons exist; use them if a word here is still foreign, then come back.

## 3. Precise statement

Discrete conditional: $p_{X\mid Y}(x\mid y)=p(x,y)/p_Y(y)$ for $p_Y(y)>0$. Continuous: $f_{X\mid Y}(x\mid y)=f(x,y)/f_Y(y)$. Each conditional is a probability distribution in the remaining variable.

Copy that statement onto paper and replace each symbol with a tiny example. A definition you cannot instantiate is a definition you do not have.

## 4. How a working mathematician uses it

Write the hypotheses, apply the move, check the conclusion against the question asked.

On the exam, this should feel like a reflex: see the pattern, apply the move, check hypotheses, box the quantity asked for.

## 5. Worked example

**Problem.** From $p(0,0)=0.1$, $p(0,1)=0.2$, $p(1,0)=0.3$, $p(1,1)=0.4$, find $P(X=1\mid Y=0)$.

**Solution.** $p_Y(0)=0.4$, so $0.3/0.4=3/4$.

Now cover the solution and redo it. If you cannot, you are still in Section 2.

## 6. GRE-style problem

**Problem.** $f(x,y)=2$ on $0<x<y<1$. $f_Y(y)$ and $f_{X\mid Y}(x\mid y)$.

**Solution.** $f_Y(y)=\int_0^y 2\,dx=2y$ on $(0,1)$. Then $f_{X\mid Y}=2/(2y)=1/y$ on $0<x<y$ (uniform on $(0,y)$).

Notice what was *not* done: no extra theory, no calculator, no essay. The governing idea, then the computation.

## 7. Traps that cost points

- Using $p(x,y)/p_X(x)$ when the condition is $Y=y$ (that's $p_{Y\mid X}$).
- Skip checking a candidate in the original equation.

## 8. Drills

Work these closed-book. Then check.

**Drill 1.** $P(Y=1\mid X=1)$ in the table $0.1,0.2,0.3,0.4$.

**Solution.** $0.4/0.7=4/7$.

Cover that solution, redo the drill on paper, then continue.

**Drill 2.** If $X,Y$ independent, $f_{X\mid Y}(x\mid y)$ equals

**Solution.** $f_X(x)$.

## 9. Lock it in

You own this skill when you can do Drill 1 and Drill 2 tomorrow, mixed among unrelated problems, in under five minutes each, with the traps named in Section 7 avoided on purpose.

Previous: [M-844](/lesson/M-844) — Marginal distributions.

Next lesson: [M-846](/lesson/M-846) — Independence of random variables.

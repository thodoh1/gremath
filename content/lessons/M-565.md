# Lesson M-565 — Kernel

Part M. M. Group Theory · **Homomorphisms & quotients** · High

Prerequisites: `M.3`.


This lesson isolates **Kernel** and trains it until it is automatic.

This is a High skill: it is standard GRE material. You should be able to do it without warming up. Group theory is the main abstract-algebra GRE diet: orders, Lagrange, homomorphisms.

## 1. Why this skill exists

If you cannot do **Kernel**, every later item that secretly uses it becomes a two-minute panic. The GRE will not name the skill. It will drop you into a computation or a “which statement is true?” and wait.

## 2. From zero

### Landscape of *Homomorphisms & quotients*

A homomorphism preserves the operation. Kernel is a normal subgroup. First isomorphism theorem: $G/\ker\phi\cong\mathrm{im}\,\phi$. Quotients exist precisely because of normality.

### This skill, specifically

A homomorphism preserves the operation.

You are not learning the whole topic again. You are learning the exact move named in the title. Neighboring lessons exist; use them if a word here is still foreign, then come back.

## 3. Precise statement

The kernel of $\phi:G\to K$ is $\ker\phi=\{g\in G:\phi(g)=e_K\}$. It is always a normal subgroup of $G$. $\phi$ is injective iff $\ker\phi=\{e\}$. Computation: $\phi(a)=\phi(b)$ iff $a^{-1}b\in\ker\phi$. Examples: $\ker\det=SL_n$, $\ker\mathrm{sgn}=A_n$, $\ker(\mathbb{Z}\to\mathbb{Z}/n\mathbb{Z})=n\mathbb{Z}$.

Copy that statement onto paper and replace each symbol with a tiny example. A definition you cannot instantiate is a definition you do not have.

## 4. How a working mathematician uses it

Write the hypotheses, apply the move, check the conclusion against the question asked.

On the exam, this should feel like a reflex: see the pattern, apply the move, check hypotheses, box the quantity asked for.

## 5. Worked example

**Problem.** Kernel of $\phi:\mathbb{Z}\to\mathbb{Z}/6\mathbb{Z}$, $\phi(n)=2n\bmod 6$.

**Solution.** $2n\equiv 0\pmod{6}$ iff $3\mid n$, so $\ker=3\mathbb{Z}$. Note: not injective, image is $\{0,2,4\}\le\mathbb{Z}/6\mathbb{Z}$.

Now cover the solution and redo it. If you cannot, you are still in Section 2.

## 6. GRE-style problem

**Problem.** $\ker(\mathrm{sgn}:S_n\to\{\pm 1\})$

**Solution.** $A_n$.

Notice what was *not* done: no extra theory, no calculator, no essay. The governing idea, then the computation.

## 7. Traps that cost points

- Thinking the kernel is a subset of the codomain, or that $\ker\phi=H$ implies $\phi$ is the quotient map without checking surjectivity.
- Skip checking a candidate in the original equation.

## 8. Drills

Work these closed-book. Then check.

**Drill 1.** Kernel of $\exp:(\mathbb{R},+)\to\mathbb{C}^\times$.

**Solution.** $\{0\}$, since $e^x=1$ for real $x$ iff $x=0$.

Cover that solution, redo the drill on paper, then continue.

**Drill 2.** If $\ker\phi=G$, then $\phi$ is

**Solution.** the trivial homomorphism.

## 9. Lock it in

You own this skill when you can do Drill 1 and Drill 2 tomorrow, mixed among unrelated problems, in under five minutes each, with the traps named in Section 7 avoided on purpose.

Previous: [M-564](/lesson/M-564) — Group homomorphism.

Next lesson: [M-566](/lesson/M-566) — Image.

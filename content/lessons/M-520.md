# Lesson M-520 — Multiplicative order

Part L. L. Number Theory · **Euler/Fermat tools** · High

Prerequisites: `L.2`.


This lesson isolates **Multiplicative order** and trains it until it is automatic.

This is a High skill: it is standard GRE material. You should be able to do it without warming up. Number theory is additional-topics / algebra: congruences, Euler, CRT.

## 1. Why this skill exists

If you cannot do **Multiplicative order**, every later item that secretly uses it becomes a two-minute panic. The GRE will not name the skill. It will drop you into a computation or a “which statement is true?” and wait.

## 2. From zero

### Landscape of *Euler/Fermat tools*

Fermat: $a^{p-1}\equiv 1\pmod p$ if $p\nmid a$. Euler: $a^{\phi(n)}\equiv 1\pmod n$ if $\gcd(a,n)=1$. $\phi$ is multiplicative. Orders divide $\phi(n)$.

### This skill, specifically

Fermat: $a^{p-1}\equiv 1\pmod p$ if $p\nmid a$.

You are not learning the whole topic again. You are learning the exact move named in the title. Neighboring lessons exist; use them if a word here is still foreign, then come back.

## 3. Precise statement

The multiplicative order $\mathrm{ord}_n(a)$ of $a$ coprime to $n$ is the least positive $k$ with $a^k\equiv 1\pmod{n}$. It divides $\phi(n)$ (Lagrange in $(\mathbb{Z}/n\mathbb{Z})^\times$). $a^m\equiv 1$ iff $\mathrm{ord}_n(a)\mid m$. $a^i\equiv a^j$ iff $\mathrm{ord}\mid(i-j)$.

Copy that statement onto paper and replace each symbol with a tiny example. A definition you cannot instantiate is a definition you do not have.

## 4. How a working mathematician uses it

Write the hypotheses, apply the move, check the conclusion against the question asked.

On the exam, this should feel like a reflex: see the pattern, apply the move, check hypotheses, box the quantity asked for.

## 5. Worked example

**Problem.** Find $\mathrm{ord}_9(2)$.

**Solution.** $\gcd(2,9)=1$, $\phi(9)=6$ so order divides $6$. $2^1=2$, $2^2=4$, $2^3=8\equiv -1$, $2^6\equiv 1$. Least positive is $6$.

Now cover the solution and redo it. If you cannot, you are still in Section 2.

## 6. GRE-style problem

**Problem.** Order of $3$ mod $7$

**Solution.** $6$: $3,2,6,4,5,1$. A primitive root.

Notice what was *not* done: no extra theory, no calculator, no essay. The governing idea, then the computation.

## 7. Traps that cost points

- Calling the order $n$ (the modulus) rather than a divisor of $\phi(n)$.
- Skip checking a candidate in the original equation.

## 8. Drills

Work these closed-book. Then check.

**Drill 1.** Order of $4$ mod $7$.

**Solution.** $4^1=4$, $4^2=2$, $4^3=1\pmod{7}$, order $3$.

Cover that solution, redo the drill on paper, then continue.

**Drill 2.** If $a^6\equiv 1\pmod{n}$ and $a^2\not\equiv 1$, $a^3\not\equiv 1$, the order is

**Solution.** $6$ (divisors of $6$ are $1,2,3,6$).

## 9. Lock it in

You own this skill when you can do Drill 1 and Drill 2 tomorrow, mixed among unrelated problems, in under five minutes each, with the traps named in Section 7 avoided on purpose.

Previous: [M-519](/lesson/M-519) — Euler's theorem.

Next lesson: [M-521](/lesson/M-521) — Units modulo n.

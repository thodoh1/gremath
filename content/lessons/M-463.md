# Lesson M-463 — Matrix representation

Part K. K. Linear Algebra · **Linear transformations** · Critical

Prerequisites: `K.4`.


This lesson isolates **Matrix representation** and trains it until it is automatic.

This is a Critical skill: it is used constantly, and a hole here leaks points all over the paper. Linear algebra is a large share of the algebra portion. Rank, kernels, eigenvalues, and determinants are exam staples.

## 1. Why this skill exists

If you cannot do **Matrix representation**, every later item that secretly uses it becomes a two-minute panic. The GRE will not name the skill. It will drop you into a computation or a “which statement is true?” and wait.

## 2. From zero

### Landscape of *Linear transformations*

$T(au+bv)=aT(u)+bT(v)$. Kernel and image, injectivity/surjectivity, matrices of $T$, change of basis, similarity. Invertible iff kernel is trivial (in finite dimension, iff onto).

### This skill, specifically

$T(au+bv)=aT(u)+bT(v)$.

You are not learning the whole topic again. You are learning the exact move named in the title. Neighboring lessons exist; use them if a word here is still foreign, then come back.

## 3. Precise statement

Relative to bases $\mathcal{B}$ of $V$ and $\mathcal{C}$ of $W$, a linear $T:V\to W$ has matrix $A$ whose $j$th column is $[T(\mathbf{b}_j)]_{\mathcal{C}}$. Then $[T(\mathbf{v})]_{\mathcal{C}}=A[\mathbf{v}]_{\mathcal{B}}$. For the standard bases, the columns of $A$ are $T(\mathbf{e}_j)$.

Copy that statement onto paper and replace each symbol with a tiny example. A definition you cannot instantiate is a definition you do not have.

## 4. How a working mathematician uses it

Write the hypotheses, apply the move, check the conclusion against the question asked.

On the exam, this should feel like a reflex: see the pattern, apply the move, check hypotheses, box the quantity asked for.

## 5. Worked example

**Problem.** Standard matrix of $T(x,y)=(2x,x+y)$.

**Solution.** $T(\mathbf{e}_1)=(2,1)$, $T(\mathbf{e}_2)=(0,1)$, so $A=\begin{pmatrix}2&0\\1&1\end{pmatrix}$.

Now cover the solution and redo it. If you cannot, you are still in Section 2.

## 6. GRE-style problem

**Problem.** The $j$th column of the standard matrix is

**Solution.** $T(\mathbf{e}_j)$.

Notice what was *not* done: no extra theory, no calculator, no essay. The governing idea, then the computation.

## 7. Traps that cost points

- Putting the images of basis vectors in rows, or using $[T(\mathbf{v})]$ without specifying bases.
- Skip checking a candidate in the original equation.

## 8. Drills

Work these closed-book. Then check.

**Drill 1.** Standard matrix of rotation by $\pi/2$ in $\mathbb{R}^2$.

**Solution.** $\begin{pmatrix}0&-1\\1&0\end{pmatrix}$.

Cover that solution, redo the drill on paper, then continue.

**Drill 2.** If $A$ is the matrix of $T$, then $T$ is invertible iff $A$ is

**Solution.** invertible.

## 9. Lock it in

You own this skill when you can do Drill 1 and Drill 2 tomorrow, mixed among unrelated problems, in under five minutes each, with the traps named in Section 7 avoided on purpose.

Previous: [M-462](/lesson/M-462) — Surjectivity.

Next lesson: [M-464](/lesson/M-464) — Composition of transformations.

# Lesson M-831 — Linearity of expectation

Part T. T. Probability · **Discrete random variables** · High

Prerequisites: `T.1`.


This lesson isolates **Linearity of expectation** and trains it until it is automatic.

This is a High skill: it is standard GRE material. You should be able to do it without warming up. Probability is a solid additional-topics block: Bayes, expectation, named distributions, CLT.

## 1. Why this skill exists

If you cannot do **Linearity of expectation**, every later item that secretly uses it becomes a two-minute panic. The GRE will not name the skill. It will drop you into a computation or a “which statement is true?” and wait.

## 2. From zero

### Landscape of *Discrete random variables*

A random variable is a function from outcomes to numbers. PMF, CDF, $E[X]$, $\mathrm{Var}(X)$, linearity (always), variance additivity (independent), indicators, covariance, correlation.

### This skill, specifically

A random variable is a function from outcomes to numbers.

You are not learning the whole topic again. You are learning the exact move named in the title. Neighboring lessons exist; use them if a word here is still foreign, then come back.

## 3. Precise statement

Linearity: $\mathbb{E}[X_1+\cdots+X_n]=\mathbb{E}[X_1]+\cdots+\mathbb{E}[X_n]$ with no independence hypothesis. This is the main reason to introduce indicators. $\mathbb{E}[cX]=c\mathbb{E}[X]$.

Copy that statement onto paper and replace each symbol with a tiny example. A definition you cannot instantiate is a definition you do not have.

## 4. How a working mathematician uses it

Write the hypotheses, apply the move, check the conclusion against the question asked.

On the exam, this should feel like a reflex: see the pattern, apply the move, check hypotheses, box the quantity asked for.

## 5. Worked example

**Problem.** Ten dice, $S=\text{sum}$. $\mathbb{E}[S]$.

**Solution.** $10\cdot 3.5=35$.

Now cover the solution and redo it. If you cannot, you are still in Section 2.

## 6. GRE-style problem

**Problem.** $n$ people, $X=\#$ of fixed points of a random permutation. $\mathbb{E}[X]$.

**Solution.** $1$ for $n\ge 1$: $X=\sum_{j=1}^n I_j$ with $I_j=\mathbf{1}_{\{\pi(j)=j\}}$, so $\mathbb{E}[I_j]=(n-1)!/n!=1/n$ and $\mathbb{E}[X]=1$.

Notice what was *not* done: no extra theory, no calculator, no essay. The governing idea, then the computation.

## 7. Traps that cost points

- Requiring independence before adding expectations, or applying the same reasoning to $\mathbb{E}[XY]$ or $\mathrm{Var}(X+Y)$.
- Skip checking a candidate in the original equation.

## 8. Drills

Work these closed-book. Then check.

**Drill 1.** $\mathbb{E}[3X-2Y+5]$ if $\mathbb{E}[X]=4$, $\mathbb{E}[Y]=7$.

**Solution.** $12-14+5=3$.

Cover that solution, redo the drill on paper, then continue.

**Drill 2.** Indicators $I_A,I_B$. $\mathbb{E}[I_A+I_B]$ vs $\mathbb{E}[I_A I_B]$.

**Solution.** $P(A)+P(B)$ vs $P(A\cap B)$.

## 9. Lock it in

You own this skill when you can do Drill 1 and Drill 2 tomorrow, mixed among unrelated problems, in under five minutes each, with the traps named in Section 7 avoided on purpose.

Previous: [M-830](/lesson/M-830) — Standard deviation.

Next lesson: [M-832](/lesson/M-832) — Variance of sums under independence.

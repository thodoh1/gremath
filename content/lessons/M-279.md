# Lesson M-279 — Arc length

Part F. F. Calculus II — Integration · **Applications of integration** · High

Prerequisites: `F.2`.


This lesson isolates **Arc length** and trains it until it is automatic.

This is a High skill: it is standard GRE material. You should be able to do it without warming up. Integrals, FTC, and applications are first-class GRE citizens. Improper integrals feed series.

## 1. Why this skill exists

If you cannot do **Arc length**, every later item that secretly uses it becomes a two-minute panic. The GRE will not name the skill. It will drop you into a computation or a “which statement is true?” and wait.

## 2. From zero

### Landscape of *Applications of integration*

Area between curves, volumes (disk, washer, shell), arc length, surface of revolution, work, center of mass: each is “slice, approximate, integrate.” The GRE tests setup as much as evaluation.

### This skill, specifically

Area between curves, volumes (disk, washer, shell), arc length, surface of revolution, work, center of mass: each is “slice, approximate, integrate.

You are not learning the whole topic again. You are learning the exact move named in the title. Neighboring lessons exist; use them if a word here is still foreign, then come back.

## 3. Precise statement

Arc length of a $C^1$ graph $y=f(x)$ from $a$ to $b$ is $L=\int_a^b\sqrt{1+[f'(x)]^2}\,dx$. Parametric: $\int_a^b\sqrt{x'^2+y'^2}\,dt$. Polar: $\int\sqrt{r^2+r'^2}\,d\theta$.

Copy that statement onto paper and replace each symbol with a tiny example. A definition you cannot instantiate is a definition you do not have.

## 4. How a working mathematician uses it

Write the hypotheses, apply the move, check the conclusion against the question asked.

On the exam, this should feel like a reflex: see the pattern, apply the move, check hypotheses, box the quantity asked for.

## 5. Worked example

**Problem.** Length of $y=x$ on $[0,1]$.

**Solution.** $\int_0^1\sqrt{2}\,dx=\sqrt{2}$.

Now cover the solution and redo it. If you cannot, you are still in Section 2.

## 6. GRE-style problem

**Problem.** Length of the semicircle $y=\sqrt{1-x^2}$ on $[-1,1]$

**Solution.** $\pi$ (should match half circumference).

Notice what was *not* done: no extra theory, no calculator, no essay. The governing idea, then the computation.

## 7. Traps that cost points

- Forgetting the $1+$ inside the square root (that would be $\int|f'|$, not length).
- Skip checking a candidate in the original equation.

## 8. Drills

Work these closed-book. Then check.

**Drill 1.** Parametric $(\cos t,\sin t)$, $t\in[0,2\pi]$.

**Solution.** $\int_0^{2\pi} 1\,dt=2\pi$.

Cover that solution, redo the drill on paper, then continue.

**Drill 2.** $y=\ln(\sec x)$ on $[0,\pi/4]$: $1+(y')^2=\sec^2$, length

**Solution.** $\int_0^{\pi/4}\sec x\,dx=\ln(\sqrt{2}+1)$.

## 9. Lock it in

You own this skill when you can do Drill 1 and Drill 2 tomorrow, mixed among unrelated problems, in under five minutes each, with the traps named in Section 7 avoided on purpose.

Previous: [M-278](/lesson/M-278) — Volumes by shells.

Next lesson: [M-280](/lesson/M-280) — Surface area of revolution.

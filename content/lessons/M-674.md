# Lesson M-674 — Interior

Part P. P. Topology · **Metric/topological basics** · Medium

Prerequisites: `O.1-O.4`.


This lesson isolates **Interior** and trains it until it is automatic.

This is a Medium skill: it appears less often, but when it appears it is often a discriminator. Topology appears as compactness, connectedness, open/closed, Heine–Borel.

## 1. Why this skill exists

If you cannot do **Interior**, every later item that secretly uses it becomes a two-minute panic. The GRE will not name the skill. It will drop you into a computation or a “which statement is true?” and wait.

## 2. From zero

### Landscape of *Metric/topological basics*

A metric measures distance. Open balls generate open sets. Closed sets contain their limits. Interior, closure, boundary, limit points. Completeness: Cauchy sequences converge.

### This skill, specifically

A metric measures distance.

You are not learning the whole topic again. You are learning the exact move named in the title. Neighboring lessons exist; use them if a word here is still foreign, then come back.

## 3. Precise statement

The interior $\mathrm{int}(A)$ is the largest open set contained in $A$, equivalently $\{x\in A:\exists r>0,\, B(x,r)\subset A\}$. Always $\mathrm{int}(\mathrm{int} A)=\mathrm{int} A$. $\mathrm{int}[0,1]=(0,1)$, $\mathrm{int}\mathbb{Q}=\emptyset$ in $\mathbb{R}$.

Copy that statement onto paper and replace each symbol with a tiny example. A definition you cannot instantiate is a definition you do not have.

## 4. How a working mathematician uses it

Write the hypotheses, apply the move, check the conclusion against the question asked.

On the exam, this should feel like a reflex: see the pattern, apply the move, check hypotheses, box the quantity asked for.

## 5. Worked example

**Problem.** Find $\mathrm{int}((0,1]\cup\{2\})$ in $\mathbb{R}$.

**Solution.** $(0,1)$: the point $1$ is not interior, and $2$ is isolated from the interval but still not interior in $\mathbb{R}$.

Now cover the solution and redo it. If you cannot, you are still in Section 2.

## 6. GRE-style problem

**Problem.** $\mathrm{int}\mathbb{Z}$ in $\mathbb{R}$ equals

**Solution.** $\emptyset$.

Notice what was *not* done: no extra theory, no calculator, no essay. The governing idea, then the computation.

## 7. Traps that cost points

- Calling an endpoint of a closed interval an interior point of that interval in $\mathbb{R}$.
- Skip checking a candidate in the original equation.

## 8. Drills

Work these closed-book. Then check.

**Drill 1.** $\mathrm{int}[0,1]$ in $\mathbb{R}$.

**Solution.** $(0,1)$.

Cover that solution, redo the drill on paper, then continue.

**Drill 2.** Interior of a set relative to itself (subspace $[0,1]$): $\mathrm{int}_{[0,1]}[0,1]$.

**Solution.** $[0,1]$, the whole space.

## 9. Lock it in

You own this skill when you can do Drill 1 and Drill 2 tomorrow, mixed among unrelated problems, in under five minutes each, with the traps named in Section 7 avoided on purpose.

Previous: [M-673](/lesson/M-673) — Neighborhoods.

Next lesson: [M-675](/lesson/M-675) — Closure.

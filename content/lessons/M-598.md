# Lesson M-598 — Polynomial rings R[x]

Part N. N. Rings, Modules & Fields · **Polynomial rings & factorization** · Medium

Prerequisites: `N.1-N.2`.


This lesson isolates **Polynomial rings R[x]** and trains it until it is automatic.

This is a Medium skill: it appears less often, but when it appears it is often a discriminator. Rings and fields appear as recognition and finite examples, especially $\mathbb{Z}/n\mathbb{Z}$ and $F[x]$.

## 1. Why this skill exists

If you cannot do **Polynomial rings R[x]**, every later item that secretly uses it becomes a two-minute panic. The GRE will not name the skill. It will drop you into a computation or a “which statement is true?” and wait.

## 2. From zero

### Landscape of *Polynomial rings & factorization*

$F[x]$ is a Euclidean domain, hence a PID and UFD. Irreducibility tests, rational root theorem, division. CRT for rings: $R/(I\cap J)\cong R/I\times R/J$ when $I+J=R$.

### This skill, specifically

$F[x]$ is a Euclidean domain, hence a PID and UFD.

You are not learning the whole topic again. You are learning the exact move named in the title. Neighboring lessons exist; use them if a word here is still foreign, then come back.

## 3. Precise statement

The polynomial ring $R[x]$ consists of polynomials $\sum_{k=0}^n a_k x^k$ with $a_k\in R$, addition coefficientwise, multiplication by Cauchy product. If $R$ is a commutative domain then so is $R[x]$, and $\deg(fg)=\deg f+\deg g$. $R[x]^\times=R^\times$ when $R$ is a reduced domain (e.g. a field or $\mathbb{Z}$). Evaluation at $a\in R$ is a hom $R[x]\to R$ if $R$ is commutative.

Copy that statement onto paper and replace each symbol with a tiny example. A definition you cannot instantiate is a definition you do not have.

## 4. How a working mathematician uses it

Write the hypotheses, apply the move, check the conclusion against the question asked.

On the exam, this should feel like a reflex: see the pattern, apply the move, check hypotheses, box the quantity asked for.

## 5. Worked example

**Problem.** Degree of $(x^2+1)(x^3-x)$ in $\mathbb{Z}[x]$, and units of $\mathbb{Q}[x]$.

**Solution.** Degrees add: $5$. Units of $\mathbb{Q}[x]$ are the nonzero constants $\mathbb{Q}^\times$.

Now cover the solution and redo it. If you cannot, you are still in Section 2.

## 6. GRE-style problem

**Problem.** Is $\mathbb{Z}[x]$ a PID?

**Solution.** No: $(2,x)$ is not principal.

Notice what was *not* done: no extra theory, no calculator, no essay. The governing idea, then the computation.

## 7. Traps that cost points

- Treating $R[x]$ as a field when $R$ is a field (the field of fractions is $R(x)$, rational functions).
- Skip checking a candidate in the original equation.

## 8. Drills

Work these closed-book. Then check.

**Drill 1.** Constant term of $(1+2x)(3-x)$ in $\mathbb{Z}[x]$.

**Solution.** $3$.

Cover that solution, redo the drill on paper, then continue.

**Drill 2.** If $R$ has zero divisors, can $\deg(fg)<\deg f+\deg g$?

**Solution.** Yes: $(2x)(3x)=0$ in $(\mathbb{Z}/6)[x]$.

## 9. Lock it in

You own this skill when you can do Drill 1 and Drill 2 tomorrow, mixed among unrelated problems, in under five minutes each, with the traps named in Section 7 avoided on purpose.

Previous: [M-597](/lesson/M-597) — Polynomial ideals basics.

Next lesson: [M-599](/lesson/M-599) — Polynomial division.

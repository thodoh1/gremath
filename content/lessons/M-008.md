# Lesson M-008 — Upper/lower bounds

Part A. A. Foundations & Algebraic Fluency · **Arithmetic & number manipulation** · Critical

Prerequisites: `none — this is ground floor`.


Series and integrals are won by bounds, not by closed forms.

This is a Critical skill: it is used constantly, and a hole here leaks points all over the paper. Algebraic fluency is not a listed ETS bucket of its own, but it is the cost of every calculus and algebra item. Expect it to be invisible when you have it and fatal when you do not.

## 1. Why this skill exists

If you cannot do **Upper/lower bounds**, every later item that secretly uses it becomes a two-minute panic. The GRE will not name the skill. It will drop you into a computation or a “which statement is true?” and wait.

## 2. From zero

### Landscape of *Arithmetic & number manipulation*

Arithmetic is the field operations on $\mathbb{Z}\subset\mathbb{Q}\subset\mathbb{R}$. Integers add and multiply and subtract; rationals add division (except by zero); reals add limits of rationals, which you will meet as completeness in Part O. On the GRE there is no calculator, so every later topic — limits, matrices, congruences, probabilities — is only as strong as this fluency. Train until a fraction, a sign, a floor, and a bound are boring.

### This skill, specifically

Bounding is the grown-up version of estimation: replace a monster by something larger you can compute. Later, $\sup$ is the least upper bound.

You are not learning the whole topic again. You are learning the exact move named in the title. Neighboring lessons exist; use them if a word here is still foreign, then come back.

## 3. Precise statement

A number $M$ is an upper bound for a set $S\subset\mathbb{R}$ if $x\le M$ for all $x\in S$. A lower bound $m$ satisfies $x\ge m$ for all $x\in S$. Bounds need not be achieved and need not be unique.

Copy that statement onto paper and replace each symbol with a tiny example. A definition you cannot instantiate is a definition you do not have.

## 4. How a working mathematician uses it

To bound a positive product, bound each factor in the direction that preserves the inequality. For $1/(1+x)$ with $x>0$, $1/(1+x)<1$.

On the exam, this should feel like a reflex: see the pattern, apply the move, check hypotheses, box the quantity asked for.

## 5. Worked example

**Problem.** Show $n/(n+1)<1$ for $n>0$.

**Solution.** $n<n+1$, divide by the positive $n+1$.

Now cover the solution and redo it. If you cannot, you are still in Section 2.

## 6. GRE-style problem

**Problem.** For $x>0$, a valid upper bound for $\\dfrac{x}{1+x}$ is

**Solution.** $1$, since $x<1+x$. Also $x$ itself is an upper bound only if $x\\ge x/(1+x)$, which is true, but $1$ is the GRE-friendly bound.

Notice what was *not* done: no extra theory, no calculator, no essay. The governing idea, then the computation.

## 7. Traps that cost points

- Reversing inequalities when taking reciprocals of negatives.
- Thinking a bound must be the maximum.

## 8. Drills

Work these closed-book. Then check.

**Drill 1.** Give a lower bound for $n^2+1$ when $n\\in\\mathbb{Z}$.

**Solution.** $1$, achieved at $n=0$.

Cover that solution, redo the drill on paper, then continue.

**Drill 2.** Is $2$ an upper bound for $\{1-1/n:n\\in\\mathbb{N}\}$?

**Solution.** Yes (so is $1$). $1$ is the least upper bound.

## 9. Lock it in

You own this skill when you can do Drill 1 and Drill 2 tomorrow, mixed among unrelated problems, in under five minutes each, with the traps named in Section 7 avoided on purpose.

Previous: [M-007](/lesson/M-007) — Estimation and sensible rounding.

Next lesson: [M-009](/lesson/M-009) — Dimensional analysis and unit consistency.

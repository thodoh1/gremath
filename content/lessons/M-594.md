# Lesson M-594 — Maximal ideals

Part N. N. Rings, Modules & Fields · **Ideals & quotient rings** · Medium

Prerequisites: `N.1`.


This lesson isolates **Maximal ideals** and trains it until it is automatic.

This is a Medium skill: it appears less often, but when it appears it is often a discriminator. Rings and fields appear as recognition and finite examples, especially $\mathbb{Z}/n\mathbb{Z}$ and $F[x]$.

## 1. Why this skill exists

If you cannot do **Maximal ideals**, every later item that secretly uses it becomes a two-minute panic. The GRE will not name the skill. It will drop you into a computation or a “which statement is true?” and wait.

## 2. From zero

### Landscape of *Ideals & quotient rings*

Ideals are kernels of ring maps. Principal, prime, maximal. $R/I$ is a field iff $I$ is maximal (in commutative unital rings). Polynomial ideals in $F[x]$ are principal.

### This skill, specifically

Ideals are kernels of ring maps.

You are not learning the whole topic again. You are learning the exact move named in the title. Neighboring lessons exist; use them if a word here is still foreign, then come back.

## 3. Precise statement

A proper ideal $M$ is maximal if no ideal lies strictly between $M$ and $R$. Equivalent (commutative unital): $R/M$ is a field. Maximal $\Rightarrow$ prime. In $\mathbb{Z}$, maximal ideals are $(p)$ for $p$ prime (so $(0)$ is prime but not maximal). In $F[x]$, $(f)$ is maximal iff $f$ is irreducible.

Copy that statement onto paper and replace each symbol with a tiny example. A definition you cannot instantiate is a definition you do not have.

## 4. How a working mathematician uses it

Write the hypotheses, apply the move, check the conclusion against the question asked.

On the exam, this should feel like a reflex: see the pattern, apply the move, check hypotheses, box the quantity asked for.

## 5. Worked example

**Problem.** Is $(x)$ maximal in $\mathbb{R}[x]$? In $\mathbb{Z}[x]$?

**Solution.** $\mathbb{R}[x]/(x)\cong\mathbb{R}$, a field: yes. $\mathbb{Z}[x]/(x)\cong\mathbb{Z}$, not a field: $(x)$ is prime not maximal. Strictly larger: $(x,2)$.

Now cover the solution and redo it. If you cannot, you are still in Section 2.

## 6. GRE-style problem

**Problem.** $R/M$ is a field iff

**Solution.** $M$ is maximal.

Notice what was *not* done: no extra theory, no calculator, no essay. The governing idea, then the computation.

## 7. Traps that cost points

- Thinking $(0)$ is maximal in $\mathbb{Z}$, or that prime $\Rightarrow$ maximal always (true in PIDs that are not fields? In a PID, nonzero prime ideals are maximal).
- Skip checking a candidate in the original equation.

## 8. Drills

Work these closed-book. Then check.

**Drill 1.** Maximal ideals of $\mathbb{Z}$.

**Solution.** $(p)$ for $p$ prime.

Cover that solution, redo the drill on paper, then continue.

**Drill 2.** Is $(x^2+1)$ maximal in $\mathbb{R}[x]$?

**Solution.** Yes: quotient $\cong\mathbb{C}$, a field. Irreducible over $\mathbb{R}$.

## 9. Lock it in

You own this skill when you can do Drill 1 and Drill 2 tomorrow, mixed among unrelated problems, in under five minutes each, with the traps named in Section 7 avoided on purpose.

Previous: [M-593](/lesson/M-593) — Prime ideals.

Next lesson: [M-595](/lesson/M-595) — Quotient rings.

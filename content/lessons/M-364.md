# Lesson M-364 — Gradient fields

Part I. I. Vector Calculus · **Vector fields** · High

Prerequisites: `H.2`.


This lesson isolates **Gradient fields** and trains it until it is automatic.

This is a High skill: it is standard GRE material. You should be able to do it without warming up. Vector calculus (Green/Stokes/divergence) appears less often than single-variable calculus but is a high-leverage discriminator.

## 1. Why this skill exists

If you cannot do **Gradient fields**, every later item that secretly uses it becomes a two-minute panic. The GRE will not name the skill. It will drop you into a computation or a “which statement is true?” and wait.

## 2. From zero

### Landscape of *Vector fields*

A vector field assigns a vector to each point. Gradients are special fields. Velocity and acceleration are derivatives of vector-valued paths.

### This skill, specifically

A vector field assigns a vector to each point.

You are not learning the whole topic again. You are learning the exact move named in the title. Neighboring lessons exist; use them if a word here is still foreign, then come back.

## 3. Precise statement

A gradient field is $\mathbf{F}=\nabla f$ for a scalar $f$ (a potential). Then $P=f_x$, $Q=f_y$, $R=f_z$. Flow lines of $\nabla f$ are orthogonal to level sets of $f$, and $\nabla f$ points toward increase of $f$.

Copy that statement onto paper and replace each symbol with a tiny example. A definition you cannot instantiate is a definition you do not have.

## 4. How a working mathematician uses it

Write the hypotheses, apply the move, check the conclusion against the question asked.

On the exam, this should feel like a reflex: see the pattern, apply the move, check hypotheses, box the quantity asked for.

## 5. Worked example

**Problem.** If $f=x^2 y+3z$, compute $\nabla f$ at $(2,1,0)$.

**Solution.** $\nabla f=(2xy,x^2,3)$, so $(4,4,3)$.

Now cover the solution and redo it. If you cannot, you are still in Section 2.

## 6. GRE-style problem

**Problem.** Which field is $\nabla(xy^2)$?

**Solution.** $(y^2,2xy)$.

Notice what was *not* done: no extra theory, no calculator, no essay. The governing idea, then the computation.

## 7. Traps that cost points

- Calling every field with $P_y=Q_x$ a gradient field without a simply connected domain (e.g. $(-y/r^2,x/r^2)$ on $\mathbb{R}^2\setminus\{\mathbf{0}\}$).
- Skip checking a candidate in the original equation.

## 8. Drills

Work these closed-book. Then check.

**Drill 1.** $\nabla(e^{x}\sin y)$ at $(0,\pi/2)$.

**Solution.** $(1,0)$.

Cover that solution, redo the drill on paper, then continue.

**Drill 2.** Is $\mathbf{F}=(-y,x)$ a gradient field on $\mathbb{R}^2$?

**Solution.** No: $Q_x-P_y=2\neq 0$.

## 9. Lock it in

You own this skill when you can do Drill 1 and Drill 2 tomorrow, mixed among unrelated problems, in under five minutes each, with the traps named in Section 7 avoided on purpose.

Previous: [M-363](/lesson/M-363) — Vector fields.

Next lesson: [M-365](/lesson/M-365) — Directional derivative as gradient dot unit vector.

# Lesson M-615 — Subfields

Part N. N. Rings, Modules & Fields · **Fields & extensions** · Medium

Prerequisites: `N.3-L.2`.


This lesson isolates **Subfields** and trains it until it is automatic.

This is a Medium skill: it appears less often, but when it appears it is often a discriminator. Rings and fields appear as recognition and finite examples, especially $\mathbb{Z}/n\mathbb{Z}$ and $F[x]$.

## 1. Why this skill exists

If you cannot do **Subfields**, every later item that secretly uses it becomes a two-minute panic. The GRE will not name the skill. It will drop you into a computation or a “which statement is true?” and wait.

## 2. From zero

### Landscape of *Fields & extensions*

A field has every nonzero element invertible. Extensions $K/F$ have degree $[K:F]=\dim_F K$. Algebraic vs transcendental, minimal polynomials, splitting fields, $\mathbb{F}_p$ and $\mathbb{F}_{p^n}$.

### This skill, specifically

A field has every nonzero element invertible.

You are not learning the whole topic again. You are learning the exact move named in the title. Neighboring lessons exist; use them if a word here is still foreign, then come back.

## 3. Precise statement

A subfield $K\subseteq F$ is a subset that is a field under the same operations (hence contains $0,1$ and is closed under $+,-,\cdot$, and inverses of nonzero elements). The intersection of subfields is a subfield. Prime field: smallest subfield, isomorphic to $\mathbb{Q}$ or $\mathbb{F}_p$. $\mathbb{Q}\subset\mathbb{R}\subset\mathbb{C}$. Intermediate fields of $\mathbb{C}/\mathbb{Q}$ are abundant.

Copy that statement onto paper and replace each symbol with a tiny example. A definition you cannot instantiate is a definition you do not have.

## 4. How a working mathematician uses it

Write the hypotheses, apply the move, check the conclusion against the question asked.

On the exam, this should feel like a reflex: see the pattern, apply the move, check hypotheses, box the quantity asked for.

## 5. Worked example

**Problem.** Is $\mathbb{Q}(\sqrt{2})=\{a+b\sqrt{2}:a,b\in\mathbb{Q}\}$ a subfield of $\mathbb{R}$? Inverse of $3+2\sqrt{2}$?

**Solution.** Yes: closed, and $(3+2\sqrt{2})^{-1}=(3-2\sqrt{2})/(9-8)=3-2\sqrt{2}$.

Now cover the solution and redo it. If you cannot, you are still in Section 2.

## 6. GRE-style problem

**Problem.** Prime field of $\mathbb{R}$

**Solution.** $\mathbb{Q}$. Prime field of $\mathbb{F}_{p^n}$ is $\mathbb{F}_p$.

Notice what was *not* done: no extra theory, no calculator, no essay. The governing idea, then the computation.

## 7. Traps that cost points

- Checking closure under addition and multiplication but not inverses (e.g. $\mathbb{Z}$ in $\mathbb{Q}$).
- Skip checking a candidate in the original equation.

## 8. Drills

Work these closed-book. Then check.

**Drill 1.** Is $\mathbb{Z}[i]$ a subfield of $\mathbb{C}$?

**Solution.** No: $2$ has no inverse in $\mathbb{Z}[i]$. $\mathbb{Q}(i)$ is the subfield.

Cover that solution, redo the drill on paper, then continue.

**Drill 2.** Intersection $\mathbb{Q}(\sqrt{2})\cap\mathbb{Q}(\sqrt{3})$.

**Solution.** $\mathbb{Q}$.

## 9. Lock it in

You own this skill when you can do Drill 1 and Drill 2 tomorrow, mixed among unrelated problems, in under five minutes each, with the traps named in Section 7 avoided on purpose.

Previous: [M-614](/lesson/M-614) — Field definition.

Next lesson: [M-616](/lesson/M-616) — Field extensions.

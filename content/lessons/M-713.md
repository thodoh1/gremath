# Lesson M-713 — Negating quantified statements

Part Q. Q. Logic, Sets & Proof · **Predicate logic** · High

Prerequisites: `Q.1`.


This lesson isolates **Negating quantified statements** and trains it until it is automatic.

This is a High skill: it is standard GRE material. You should be able to do it without warming up. Logic and proof are how other items are worded. Counting and analysis both depend on quantifiers.

## 1. Why this skill exists

If you cannot do **Negating quantified statements**, every later item that secretly uses it becomes a two-minute panic. The GRE will not name the skill. It will drop you into a computation or a “which statement is true?” and wait.

## 2. From zero

### Landscape of *Predicate logic*

Predicates, $\forall,\exists$, nested quantifiers, negation rules ($\neg\forall=\exists\neg$). Necessary vs sufficient is $\to$ direction. This is how $\varepsilon$-$\delta$ is written.

### This skill, specifically

Predicates, $\forall,\exists$, nested quantifiers, negation rules ($\neg\forall=\exists\neg$).

You are not learning the whole topic again. You are learning the exact move named in the title. Neighboring lessons exist; use them if a word here is still foreign, then come back.

## 3. Precise statement

Negating quantifiers: $\neg\forall x\,P(x)\equiv\exists x\,\neg P(x)$ and $\neg\exists x\,P(x)\equiv\forall x\,\neg P(x)$. Nested: $\neg\forall x\exists y\,R(x,y)\equiv\exists x\forall y\,\neg R(x,y)$. Push $\neg$ inward, flipping each quantifier, until it hits the matrix.

Copy that statement onto paper and replace each symbol with a tiny example. A definition you cannot instantiate is a definition you do not have.

## 4. How a working mathematician uses it

Write the hypotheses, apply the move, check the conclusion against the question asked.

On the exam, this should feel like a reflex: see the pattern, apply the move, check hypotheses, box the quantity asked for.

## 5. Worked example

**Problem.** Negate ``every continuous function on $[0,1]$ is bounded'' as a quantified statement.

**Solution.** There exists a continuous $f:[0,1]\to\mathbb{R}$ that is unbounded. (This negation is false by EVT; the original is true.)

Now cover the solution and redo it. If you cannot, you are still in Section 2.

## 6. GRE-style problem

**Problem.** $\neg\exists x\forall y,\, x\le y$ over $\mathbb{R}$ becomes

**Solution.** $\forall x\exists y,\, y<x$ (no least real).

Notice what was *not* done: no extra theory, no calculator, no essay. The governing idea, then the computation.

## 7. Traps that cost points

- Stopping after flipping only the first quantifier, or turning $\forall$ into $\exists$ without negating the predicate.
- Skip checking a candidate in the original equation.

## 8. Drills

Work these closed-book. Then check.

**Drill 1.** Negate $\forall\varepsilon>0\,\exists N\,\forall n\ge N,\, |a_n|<\varepsilon$.

**Solution.** $\exists\varepsilon>0\,\forall N\,\exists n\ge N,\, |a_n|\ge\varepsilon$.

Cover that solution, redo the drill on paper, then continue.

**Drill 2.** ``Not all swans are white'' means

**Solution.** there exists a nonwhite swan, not ``all swans are nonwhite''.

## 9. Lock it in

You own this skill when you can do Drill 1 and Drill 2 tomorrow, mixed among unrelated problems, in under five minutes each, with the traps named in Section 7 avoided on purpose.

Previous: [M-712](/lesson/M-712) — Existential quantifier.

Next lesson: [M-714](/lesson/M-714) — Nested quantifiers.
